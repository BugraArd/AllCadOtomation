# Project-local Codex settings for this KiCad automation project.
# Keeps Codex productive inside the repo while still asking before actions that
# leave the workspace, use the network, or need broader privileges.

approval_policy = "on-request"
sandbox_mode = "workspace-write"

[sandbox_workspace_write]
writable_roots = [
  'C:\Users\ardaa\OneDrive\Desktop\Kicad',
  'C:\tmp',
]
network_access = false
